<!DOCTYPE HTML>
<html>
<head>
    <title>Movie - Update</title>
</head>
<body>
<h1>Update Movie</h1>
<form action="/movie/update" method = "get">
    <input type="hidden"
           id="movie_id"
           name="movie_id"
           value="<%= movie_result.movie_id %>"
    />
    Movie Name: <input id="movie_name" name="movie_name" type="text" value="<%= movie_result.movie_name%>"/>

    Rating:
    <select name="movie_rating">
        <% for(var i=0; movie_rating_result.length > i; i++)
        {
            var selected ="";
            if(movie_rating_result[i].rating_number == movie_result.rating_number)
            {
                selected = "selected";

            }
        %>
            <option value="<%=movie_rating_result[i].rating_number%>" <%=selected%> >
                <%=movie_rating_result[i].rating_number%> </option>
        <%} %>

    </select>
    <br />
    <br />
    Date Watched: <input id="date_watched" name="date_watched" type="text" value="<%= movie_result.date_watched%>"/>
    <input type="submit" />
</form>
</body>
</html>